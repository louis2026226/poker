# 版本约定

**每次更新都要提供新的版本号。**

- 版本号定义在根目录 `package.json` 的 `version` 字段。
- 服务端 `/version` 接口会读取该字段并返回 `appVersion`，主页版本标签优先显示此值。
- 完成功能/修复/配置等变更后，请递增版本号（如 1.0.1 → 1.0.2），并在发布或交付时说明本次版本号。

推荐规则：
- 小修小补、文案/样式：补丁号 +1（x.y.**z**）
- 新功能、行为变更：次版本 +1（x.**y**.0）
- 不兼容大改：主版本 +1（**x**.0.0）
